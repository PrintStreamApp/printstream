# apps/web/src/plugins — domain guides

- Built-in web plugin contract → `.claude/guides/plugins.md` (implement `WebPlugin`, lazy-load routes with `React.lazy`, slot components render `null` when prerequisites are unmet, never import another plugin).
- `auth-*/` provider plugins → `.claude/guides/auth-architecture.md`.
