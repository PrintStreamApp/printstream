# apps/web/src/plugin — domain guides

This is the web plugin host (types, registry, builtin wiring, `<PluginSlot />`). Read `.claude/guides/plugins.md` before changing the plugin contract — `WebPlugin`, route/slot registration, lazy-loading, and graceful rendering when no plugin is installed.
