/**
 * Shared dimensions for compact plate-thumbnail pickers.
 *
 * The editor strip and library-backed print/slice pickers deliberately use the
 * same tile width so moving between those surfaces does not change visual scale.
 */
export const PLATE_PICKER_TILE_WIDTH_PX = 92
