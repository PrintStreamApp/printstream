import { auroraChrome, auroraTheme, defaultChrome, theme } from './theme'
import { flatThemeVariants } from './flatThemes'

export const platformChrome = defaultChrome

export const platformTheme = theme

export const platformAuroraChrome = auroraChrome

export const platformAuroraTheme = auroraTheme

export const platformFlatThemeVariants = flatThemeVariants
