// Kept as a compatibility seam for slicer-local imports. The transform is shared because the
// browser now applies it while authoring an engine-ready project.
export {
  sliceInfoCarriesNozzleGroupIds,
  stripSliceInfoNozzleGroupIds
} from '@printstream/shared/three-mf'
