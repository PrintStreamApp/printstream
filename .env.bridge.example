# Copy this file to .env on a bridge-only host.

# Public API origin the bridge should register with.
BRIDGE_CLOUD_URL=https://printstream.app

# Optional overrides (everything has a default — see the README env table for
# the full overridable list):
# BRIDGE_NAME=Store
# DISCOVERY_PORT=2021
# CAMERA_DEBUG_LOGS=false